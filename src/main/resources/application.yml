spring:
  application:
    name: user-info-ms

  datasource:
    url: jdbc:postgresql://ep-dark-lab-a4heypm7-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_SKVpHTw6E2fZ
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${SERVER_PORT:8081}
  servlet:
    context-path: /career-bridge-ms

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.example: DEBUG
    org.hibernate.SQL: DEBUG
env:
  jwt:
      secret: your-256-bit-secret-key-here-make-it-long
      expiration: 86400000